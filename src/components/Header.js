import './Header.css';
import { Col, Container, Row } from 'react-bootstrap';

const Header = () => {
	return (
		<Container className="Header">
			<p>Periodic Table</p>
		</Container>
	);
};

export default Header;
